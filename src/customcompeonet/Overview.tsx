"use client";

import * as React from "react";
import { useRouter } from "next/router";

import {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/UI/Card";

export const OverviewPage = ({}) => {
  return (
    <div
      className="grid grid-cols-1 sm:grid-cols-2 gap-0"
      style={{ gridRowGap: 350, gridColumnGap: 0 }}

      




    ></div>
  );
};
